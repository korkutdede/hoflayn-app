# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

This app targets **Expo SDK 54** so it runs in the **App Store / Play Store Expo Go** build (store Expo Go is frozen at SDK 54; SDK 55+ needs sign.expo.dev / expo.dev/go or a dev client).
